local thisVersion = _G.thisVersion
local currentVersion = "0.0.1Q"

if thisVersion == currentVersion then
Lib:Notification("Kago's GUI", "You are using new version of Kago's GUI!", "Ok")
else
game.Players:LocalPlayer:kick("Outdated Version!")
end
